# 工作區協作規則

1. 預設使用繁體中文，對沒有背景知識的使用者清楚解釋結果與下一步。
2. 若需求或設計不明確，而且會影響結果，先詢問，不猜測硬體規格或使用者意圖。
3. 保留其他工作階段與使用者的修改。使用者在當前對話的明確指示優先。

## 重要：基礎參數不得隨一般程式修改變動

使用者於 2026-09-09 明確要求保護已選定的基礎參數。開始處理控制、設定、建置或部署前，先讀：

- [重要參數基準](docs/IMPORTANT_PARAMETER_BASELINE.md)
- [固定基準副本](config/parameter_baselines/standard_20260909_r15/manifest.json)

執行唯讀檢查，並於修改／部署完成後再次核對：

```bash
python3 tools/parameter_baseline.py check
```

- 沒有使用者針對參數的明確指示，不得改動基準中的增益、PWM、摩擦補償、濾波、時間、軌跡基準／比例／方向、腿位屏蔽、到位／追蹤設定、供電與通訊保護。這也包括改共用預設值、驗證器、單位換算、控制流程、啟動覆寫或部署腳本造成的間接變更。
- 目前 Calibration／Standing 為 KP=0.35、KD=0.002、K_FF=0.02、PWM=500、摩擦=0、濾波=5ms；Standing 到位容差1000counts、settle_time_s=0、保持PWM上限300且保留阻尼。Manual 控制參數獨立、現場上限500，儲存動作計畫80不可誤認為同一個上限。
- Tripod 保留 KP=0.38、KD=0.003、K_FF=0.005、PWM=3300、8秒起步、8→1、摩擦0、濾波5ms、額外PWM slew關閉、有限位置誤差只警告、新版正常停止。原生L3持續屏蔽。完整數值以基準文件為準。
- 若檢查已存在差異，先記錄來源；不得為了讓檢查通過而擅自恢復使用者設定、改標準副本、改雜湊或重新產生基準。
- 來源／執行檔雜湊不同只表示需要審查，不等於一定改了參數。一般修復應比較快照，證明重要數值與行為未變，再交接差異；不可為匹配舊檔而撤銷其他修復。
- 使用者明確授權臨時調參時可依範圍調整並記錄；臨時調參不等於同意改永久基準。只有使用者明確要求更新標準，才建立新的有版本基準，保留舊版與變更理由。
- 不回填、偽造或復活舊 Calibration／Standing 成功紀錄；還原設定也不等於完成實體校正。revision/hash 應更新，不能直接退回15。
- `check` 與預覽不得接管設備、啟停服務、上電或運動。除非當前對話另有明確授權，開發／測試只使用離線環境。不得將過去某次現場測試授權視為永久運動授權。
- RL/Sim2Real 備用 profile、未確認的齒比／encoder／伺服比例不屬於已驗證硬體標準；不得盲目套用原生PWM或解除L3。

快速恢復僅在使用者要求恢復時執行，先用 `restore` 預覽，再用 `restore --apply` 寫入。該工具只恢復設定，不覆蓋整支程式或重启機器人服務。
