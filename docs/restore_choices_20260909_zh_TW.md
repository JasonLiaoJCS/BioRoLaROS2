# 依恢復表選擇調整的結果（2026-09-09）

已於 2026-09-09T22:34:31.901921+08:00 更新現場 revision 15。SHA256 `5866b8ee7bb6a7733c0757fb0d7dd35995a1f78e3a5ec4defe6507c130c13129`。備份：`/home/jetson/.local/state/rinbo-control-backups/restore-choices-qowhg1nw`。

92項：A 10項、B 53項、C 29項。保存使用者原始填寫檔；只整理M02漏等號、R09換行，兩者均為C。未自動上電、校正、站立、執行Tripod或策略；未啟停Bridge／Core／FPGA。

## 實際控制設定

| 模式 | KP / KD / K_FF | 摩擦 | 濾波 | 最大PWM |
|---|---|---|---|---|
| Calibration | .35 / .002 / .02 | 0 | 5ms | 500 |
| Standing 尋零／到位 | .35 / .002 / .02 | 0 | 5ms | 500 |
| Standing 保持 | .1 / .002 / 無速度前饋 | 0 | 5ms | 300（也受階段上限限制） |
| Tripod | .38 / .003 / .005 | 0 | 5ms | 3300，額外slew關閉 |
| Manual 逐腳 | .35 / .002 / .02 | 0 | 5ms | 現場500；有效值=min(計畫,現場) |

目前儲存的Manual計畫仍為L2、90°/s、加速度10°/s²、10秒、PWM80。這是待執行計畫，不表示正在動作。前饋`0.02 × 90 × 55296/360 = 276.48 PWM`已超過80；操作台新增明確提示，選13可調出力到現場500或選2降低速度。沒有自動將下次計畫改成500。Manual仍保留250 PWM/s變化率；目前10°/s²的前饋變化約30.72 PWM/s，並非僅此項就超過250。PID與負載合計仍需實機驗證。

摩擦補償選0是為了讓恢復的前饋與PD可以單獨辨識；5ms濾波是控制回饋的短時間平滑，並非已量得最佳值。原始位置／速度仍供保護，不掩蓋無效資料。

## 到位與保護

- Calibration：servo/Hall各60秒、停穩/歸零各15秒；Hall後低速<500counts/s連續0.3秒，再確認原始歸零|counts|≤100。
- Standing：平滑半圈約5.5秒；位置誤差<1000counts（按55296counts/rev約6.51°）即可完成，不再要求低速0.3秒。設定`settle_time_s=0`，若以後設正數則重新要求速度與持續時間。尋Hall/到位各60秒，保持失位>12000counts約78.13°仍停止。
- Manual：對齊/收尾2°、5°/s條件保留；持續動作誤差>12000counts仍停止，錯誤文字改為實際門檻且記錄當筆數字；既有encoder跳變/過速、SIGINT、失聯與首因保留。
- Tripod：8秒起步、8→1、名目每秒ratio減0.2，到1後持續跑；保持2秒減速/5秒停止期限。有限位置誤差仍只警告；無效回饋仍停止。
- 原生L3屏蔽、單一動作限制、Bridge PWM3300、18～42V/5筆、腿5A/25筆、馬達/電源失聯、來源及啟用ack、急停與關電契約均保留。bus30A停止原本關閉，沒有宣稱它已開啟。

## 程式與操作入口

- 現場值：`/home/jetson/redrhex_site/rinbo_fsm_disabled_leg.yaml`。新增`parameters.rinbo_manual`完整控制區塊；舊版YAML若沒有此區塊，保留原先Standing繼承行為。新區塊缺欄位或拼錯會拒絕，避免悄悄繼承。
- 參數與型別／範圍驗證：`src/rinbo_fsm/src/robot_config.cpp`；500/3300硬上界：`safety_invariants.hpp`。
- Calibration控制：`rinbo_cali.cpp`；Standing到位與保持：`rinbo_standing.cpp`；Manual獨立參數與停止文字：`rinbo_manual.cpp`；Manual計畫與最終輸出限幅：`manual_motion.hpp`。
- Control Panel：照常用`./robot.sh`；選7調上限、選13快調出力、選15調到位限制。`robot.sh`直接載入`src/rinbo_control`，新開操作台即使用新程式。舊的install內Python副本不是這個入口，本次沒有將它當成已更新的入口。
- 只讀核對：`build/rinbo_fsm/rinbo_legs status --json`。請勿用全模式`tune-motion`只改一個階段；它仍調Cali/Standing/Tripod，而新Manual控制區塊獨立。

因Calibration／Standing實際設定改變，原先成功紀錄不能沿用。部署會備份並失效舊紀錄；下次由你手動執行Calibration→Standing→Tripod。這不是自動重跑，也不是假造完成。

Windows不需改Tripod停止命令或Bridge上限：Bridge仍3300。若Windows把Cali/Standing期望值硬寫80，需改為讀取原生有效設定；目前為500。site revision/hash也應動態核對，不沿用revision14；新增rinbo_manual區塊不可被舊模板覆寫掉。這台Orin沒有Windows App原始碼，未宣稱已修改Windows。

## 每一項的處理

| 編號 | 選擇 | 項目 | 結果與理由 |
|---|---|---|---|
| C01 | A | KP 位置修正係數 | 恢復：0.35。 |
| C02 | A | KD 速度修正係數 | 恢復：0.002。 |
| C03 | A | K_FF 速度前饋 | 恢復：0.02。 |
| C04 | C | 額外摩擦補償 | 改為 0 PWM。恢復舊版前饋 0.02 後，先不疊加尚未量測的 40 PWM 摩擦補償。 |
| C05 | C | 控制速度濾波 | 改為可獨立設定的 5 ms 濾波；原始 encoder／速度仍供保護及歸零判斷。缺欄位的舊設定保留 20 ms，避免默默改動舊檔。 |
| C06 | A | 主驅動最大 PWM | 恢復：500。 |
| C07 | B | 尋 Hall 巡航速度 | 保留盤點行為／設定；其必要相依元件隨本次建置更新。 |
| C08 | C | 尋 Hall 起步曲線 | 保留 0.5 秒平滑尋零起步；最高速度仍 36°/s。恢復出力不需要同時恢復速度跳變。 |
| C09 | C | 伺服定位等待上限 | 保留 60 秒伺服定位等待。沒有等待不足的新證據，不取消逾時。 |
| C10 | C | 主馬達尋 Hall 等待上限 | 保留 60 秒尋 Hall 等待；名目每圈 10 秒已有餘裕。 |
| C11 | C | 停穩／歸零等待上限 | 保留停穩／歸零各 15 秒及有界重送，不把送出 reset 當成成功。 |
| C12 | C | Hall 後停穩與歸零完成判定 | 保留速度 <500 counts/s 持續 0.3 秒、歸零回讀 ／counts／≤100 的流程。 |
| C13 | A | 伺服中立目標／到位容差 | 舊版伺服目標與容差原本就相同，數字不變；保留已修正的帶符號差值計算，避免溢位。 |
| C14 | B | 只校正選中腳與成功紀錄 | 保留盤點行為／設定；其必要相依元件隨本次建置更新。 |
| S01 | A | KP 位置修正係數 | 恢復：0.35。 |
| S02 | A | KD 速度修正係數 | 恢復：0.002。 |
| S03 | A | K_FF 速度前饋 | 恢復：0.02。 |
| S04 | C | 額外摩擦補償 | 改為 0 PWM；理由同 C04，避免恢復較高前饋後又疊加固定摩擦補償。 |
| S05 | C | 控制速度濾波 | 改為獨立 5 ms 濾波；不改原始 encoder、比例、符號。 |
| S06 | A | 主驅動最大 PWM | 恢復：500。 |
| S07 | C | 尋 Hall／半圈軌跡 | 保留平滑尋零與半圈加減速，半圈約 5.5 秒、最高 36°/s、終點 180°。 |
| S08 | C | 到位容許位置誤差 | 保留 1000 counts（約 6.51°），先前 218 counts 誤差已在容差內；S09 另按 A 取消停穩條件。 |
| S09 | A | 到位停穩條件 | 設 settle_time_s=0，半圈參考時間結束且位置進入S08容差即完成；不要求低速度或連續0.3秒。DONE日誌清楚標示position_only。 |
| S10 | C | 尋 Hall／轉半圈等待上限 | 保留尋 Hall／半圈各 60 秒。此次主要恢復出力及到位判定，沒有根據延長到無期限。 |
| S11 | C | 到位後保持出力 | 保持控制改為 KP=min(階段 KP,0.1)=0.1，KD=0.002；保留速度阻尼，保持上限 min(階段上限,300)=300 PWM。行走到位上限仍 500。 |
| S12 | C | 保持位置遺失保護 | 保留 12000 counts（約 78.13°）保持失位停止；這是大幅失位，不是一般到位容差。 |
| S13 | B | Standing 完成與退出 | 保留盤點行為／設定；其必要相依元件隨本次建置更新。 |
| T01 | B | KP | 保留盤點行為／設定；其必要相依元件隨本次建置更新。 |
| T02 | B | KD | 保留盤點行為／設定；其必要相依元件隨本次建置更新。 |
| T03 | B | K_FF | 保留盤點行為／設定；其必要相依元件隨本次建置更新。 |
| T04 | B | 摩擦補償 | 保留盤點行為／設定；其必要相依元件隨本次建置更新。 |
| T05 | B | 速度回饋 | 保留盤點行為／設定；其必要相依元件隨本次建置更新。 |
| T06 | B | 起步時間 | 保留盤點行為／設定；其必要相依元件隨本次建置更新。 |
| T07 | B | 起步曲線／銜接 | 保留盤點行為／設定；其必要相依元件隨本次建置更新。 |
| T08 | B | 起步結束的位置基準 | 保留盤點行為／設定；其必要相依元件隨本次建置更新。 |
| T09 | B | Group B 啟動基準 | 保留盤點行為／設定；其必要相依元件隨本次建置更新。 |
| T10 | B | T-ratio 起點／目標 | 保留盤點行為／設定；其必要相依元件隨本次建置更新。 |
| T11 | B | 加速計時方式 | 保留盤點行為／設定；其必要相依元件隨本次建置更新。 |
| T12 | B | 最大PWM／額外slew | 保留盤點行為／設定；其必要相依元件隨本次建置更新。 |
| T13 | B | 步態多項式／A、B分組 | 保留盤點行為／設定；其必要相依元件隨本次建置更新。 |
| T14 | B | 負相位與跨圈 | 保留盤點行為／設定；其必要相依元件隨本次建置更新。 |
| T15 | B | 有限位置誤差保護 | 保留盤點行為／設定；其必要相依元件隨本次建置更新。 |
| T16 | C | 正常停止與執行時間 | 保留新版 2 秒減速、5 秒停止期限；到 ratio=1 後持續 RUNNING，直到停止輸入。未恢復依 callback 加 ratio 的舊停止。 |
| T17 | B | 觸發前後診斷 | 保留盤點行為／設定；其必要相依元件隨本次建置更新。 |
| G01 | B | L3及全域主驅動屏蔽 | 保留盤點行為／設定；其必要相依元件隨本次建置更新。 |
| G02 | B | 編碼器比例／方向／零點 | 保留盤點行為／設定；其必要相依元件隨本次建置更新。 |
| G03 | B | Cali/Standing反方向行程保護 | 保留盤點行為／設定；其必要相依元件隨本次建置更新。 |
| G04 | B | 供電電壓範圍 | 保留盤點行為／設定；其必要相依元件隨本次建置更新。 |
| G05 | B | 腿電流／bus電流保護 | 保留盤點行為／設定；其必要相依元件隨本次建置更新。 |
| G06 | B | 馬達／電源回讀失聯期限 | 保留盤點行為／設定；其必要相依元件隨本次建置更新。 |
| G07 | B | 來源身分／序號／QoS | 保留盤點行為／設定；其必要相依元件隨本次建置更新。 |
| G08 | B | 馬達啟用握手 | 保留盤點行為／設定；其必要相依元件隨本次建置更新。 |
| G09 | B | 唯一設定來源與可調範圍 | 保留盤點行為／設定；其必要相依元件隨本次建置更新。 |
| G10 | B | 成功紀錄與單一動作限制 | 保留盤點行為／設定；其必要相依元件隨本次建置更新。 |
| B01 | B | Core目標IP與環境變數 | 保留盤點行為／設定；其必要相依元件隨本次建置更新。 |
| B02 | B | Bridge motor PWM／servo範圍驗證 | 保留盤點行為／設定；其必要相依元件隨本次建置更新。 |
| B03 | B | 命令失聯與過期 | 保留盤點行為／設定；其必要相依元件隨本次建置更新。 |
| B04 | B | 單一發布者／rearm／ack | 保留盤點行為／設定；其必要相依元件隨本次建置更新。 |
| B05 | B | 軟體急停與上電命令epoch | 保留盤點行為／設定；其必要相依元件隨本次建置更新。 |
| B06 | B | 回讀時間戳與重播處理 | 保留盤點行為／設定；其必要相依元件隨本次建置更新。 |
| B07 | B | 停止重送／心跳／關閉 | 保留盤點行為／設定；其必要相依元件隨本次建置更新。 |
| B08 | B | 通訊主迴圈／消息數值映射 | 保留盤點行為／設定；其必要相依元件隨本次建置更新。 |
| M01 | B | 逐腳動作模式與參考 | 保留盤點行為／設定；其必要相依元件隨本次建置更新。 |
| M02 | C | 逐腳KP/KD/FF與摩擦 | 新增完整 rinbo_manual 控制區塊：KP=.35、KD=.002、K_FF=.02、摩擦0、濾波5ms。之後調 Standing 控制係數不再連動 Manual；供電／通訊仍共用 Standing 契約。 |
| M03 | C | 逐腳PWM cap與slew | Manual 現場上限與計畫允許上界改為500，保留250 PWM/s變化率。實際上限=min(計畫,現場)。目前儲存計畫80保留，選13可明確調整到500。 |
| M04 | C | 逐腳追蹤門檻／錯誤文字 | 修正實際12000卻寫5000的錯誤文字，停止當筆記錄腿名、帶符號error、target/actual counts與實際limit；門檻仍12000，無效回饋仍停止。 |
| M05 | C | 逐腳對齊完成條件 | 保留逐腳對齊／收尾位置差≤2°、速度≤5°/s；這是Manual不同於Standing的動作流程，沒有把S09選項連動過來。 |
| M06 | C | 逐腳時間／速度／加速度 | 保留你儲存的L2 90°/s、加速度10°/s²、10秒、PWM80與1～90速度／加速度範圍。新增前饋超過cap提示，不暗改已選动作。前饋約276.48 PWM，高於80；可自行用13調出力或2降速度。10°/s²的前饋增率約30.72 PWM/s，低於保留的250 slew；完整負載追蹤仍待量測。 |
| U01 | B | 文字Control Panel與步驟 | 保留盤點行為／設定；其必要相依元件隨本次建置更新。 |
| U02 | B | 第1步連線自動整理 | 保留盤點行為／設定；其必要相依元件隨本次建置更新。 |
| U03 | B | 操作台消失／子程序生命週期 | 保留盤點行為／設定；其必要相依元件隨本次建置更新。 |
| U04 | B | 正常完成／失敗後供電流程 | 保留盤點行為／設定；其必要相依元件隨本次建置更新。 |
| U05 | B | 上電前工具門檻 | 保留盤點行為／設定；其必要相依元件隨本次建置更新。 |
| U06 | B | 校正快取／重試與錯誤說明 | 保留盤點行為／設定；其必要相依元件隨本次建置更新。 |
| R01 | B | Sim2Real整體與所選profile | 保留盤點行為／設定；其必要相依元件隨本次建置更新。 |
| R02 | C | Sim2Real的腿位mask | 評估後保留未啟用的備用profile檔案；原生L3繼續屏蔽。full_feedback兩份=[]、sensor_v2兩份=[L1]並不適合直接視為本機L3設定。準備啟用RL時須指定配對profile，再依L3要求及模型觀測契約一起核對。 |
| R03 | C | RL速度到PWM轉換／限幅 | 保留目前轉換40 PWM/(rad/s)、基礎cap80與slew250；它不是Tripod的PD，沒有選定profile或速度到PWM量測，不能用原生500／3300代替。 |
| R04 | C | RL encoder／方向／伺服換算 | 保留現有數字但不認證為硬體規格；54984.83 counts/rev、ABAD1000 counts/rad仍待編碼器、減速比及實測確認。未猜測新比例。 |
| R05 | C | RL初始站姿到位 | 保留base12秒／site30秒等各自初始化到位設定；目前未選定RL啟動profile，沒有把Standing位置完成模式移植到策略控制。 |
| R06 | C | RL主驅動與ABAD動作限幅 | 保留base與site原有動作限幅；它們的速度／伺服角度單位和PWM不同，需選定模型／profile才能判定適合數字。 |
| R07 | B | RL資料、推論、姿態等保護 | 保留盤點行為／設定；其必要相依元件隨本次建置更新。 |
| R08 | B | RL policy檔案／契約／啟動使能 | 保留盤點行為／設定；其必要相依元件隨本次建置更新。 |
| R09 | C | RL供電與通訊門檻 | 保留各profile供電與通訊門檻及功能；base18～30V/3A、site18～42V/5A未被互相覆蓋。原生18～42V/5A保持。 |
| D01 | B | 資料記錄內容與CSV介面 | 保留盤點行為／設定；其必要相依元件隨本次建置更新。 |
| D02 | B | 浏览器監控／錄製 | 保留盤點行為／設定；其必要相依元件隨本次建置更新。 |
| D03 | B | ROS消息契約 | 保留盤點行為／設定；其必要相依元件隨本次建置更新。 |
| O01 | C | 舊PyQt GUI與啟動名稱 | 保留robot.sh文字操作台，不恢復PyQt GUI。你目前使用SSH／VS Code；舊GUI的Tripod執行檔名稱也不一致，沒有必要重新引入它。 |
| O02 | C | 舊PID測試／單腿RSLIP | 保留目前Manual逐腳入口及診斷，不恢復會直接發布命令的舊PID測試／單腿RSLIP程式。之後若需要特定正弦辨識實驗，再做符合現有握手的測試入口。 |
| O03 | C | IMU子模組 | 保留目前未引入microstrain子模組的狀態。沒有IMU型號、driver來源與裝置資料，不猜測安裝未知commit。 |
| O04 | B | 建置與README | 保留盤點行為／設定；其必要相依元件隨本次建置更新。 |
| F01 | B | FPGA console生命週期候選 | 保留盤點行為／設定；其必要相依元件隨本次建置更新。 |
| F02 | B | Windows期待值／停止入口 | 保留盤點行為／設定；其必要相依元件隨本次建置更新。 |

## 離線驗證與現場確認範圍

驗證涵蓋舊YAML相容、新Manual設定隔離、500PWM從設定到最終輸出、L3屏蔽、Standing位置完成模式、Calibration歸零流程、保護與停止回歸。只使用隔離ROS domain231與localhost模擬回讀；沒有啟動真實控制入口。結果與完整日誌見同目錄diagnostics/restore_choices_20260909。

Sim2Real的C項目前完成的是評估與保留決策，沒有宣稱已完成硬體參數校準。備用profile不能直接當成L3屏蔽的現場設定。要調整或啟用它，還需指定RL/lowlevel兩個profile，以及馬達／encoder型號、齒比、輸出軸一圈counts差、伺服角度與counts對應、模型觀測契約。IMU也需實際型號與驅動來源。未取得這些資料前，C不被解讀成猜一個比例。

本次離線通過不等於實機PID已調到最佳。最小下一次量測：先檢查載入係數/上限及L3；手動校正一次記錄Hall/歸零，再Standing記錄target/actual/PWM及DONE模式。若方向相反、未隨目標移動、長時間飽和而誤差持續增加，或供電／通訊報錯，使用Q／停止並保留首因日誌；不在本次自動執行。

測試結果：原生190項GTest與1項退役入口檢查通過（11個CTest套件）；Control Panel 256通過、1跳過。
